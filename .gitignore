# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Xcode / Swift
# The .xcodeproj is GENERATED by xcodegen from project.yml (the source of
# truth). Run `xcodegen generate` after cloning — see CLAUDE.md → Setup.
LHRebarAR.xcodeproj/
# Info-LH.plist is xcodegen's generated OUTPUT for the LH target's `info:`
# path (see project.yml) — same status as the .xcodeproj above. Unlike
# LHRebarAR/Info.plist (tracked, hand-authored), this one must stay untracked
# or the first `xcodegen generate` + `git add -A` on a fresh clone commits a
# second stale generated artifact.
LHRebarAR/Info-LH.plist
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.xccheckout
*.xcscmblueprint
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
timeline.xctimeline
playground.xcworkspace

# Swift Package Manager
.swiftpm/
.build/
Package.resolved
Packages/
Package.pins
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# CocoaPods (in case added later)
Pods/

# Carthage
Carthage/Build/
Carthage/Checkouts

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Code coverage
*.gcno
*.gcda

# Reality Converter scratch
*.reality

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Python (for source-model conversion scripts)
.venv/
__pycache__/
*.pyc

# Local secrets / env
.env
.env.local
secrets.xcconfig
secrets/
LHRebarAR/Services/LiveShareConfig.swift
api/livekit.env

# DB 스냅샷 (상대사 운영 데이터 — 커밋 금지)
backups/
